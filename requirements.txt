# ML-based Disease Prediction Requirements
# 
# INSTALLATION INSTRUCTIONS:
# 
# For GPU (CUDA 12.1) support - RECOMMENDED:
#   pip install --upgrade pip
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
#
# For CPU-only:
#   pip install --upgrade pip
#   pip install -r requirements.txt
#
# Verify GPU support (should print True):
#   python -c "import torch; print(torch.cuda.is_available())"

# Core ML Libraries
numpy==2.4.3
pandas==3.0.2
scikit-learn==1.8.0
joblib==1.5.3

# Deep Learning (PyTorch - install with correct CUDA index first!)
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1

# Web Dashboard
streamlit==1.56.0

# Optional visualization/utility libraries
matplotlib>=3.7.0
seaborn>=0.12.0
